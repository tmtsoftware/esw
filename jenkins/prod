pipeline {
    agent {
        label 'master'
    }

    options {
        timeout(time: 2, unit: 'HOURS')
        timestamps()
    }

    stages {
        stage('Checkout') {
            steps {
                git 'git@github.com:tmtsoftware/esw.git'
                sh "git checkout $SHA"
            }
        }

        stage('Build - Kotlin') {
            steps {
                sh "./gradle.sh clean compileKotlin"
            }
        }

        stage('Build - Scala') {
            steps {
                sh "sbt scalafmtCheck"
                sh "sbt clean makeSite"
                sh "sbt ';set every Seq(enableFatalWarnings := true, scalafmtOnCompile := false); compile'"
            }
        }

        stage('Test - Kotlin') {
            steps {
                sh "./gradle.sh clean test"
            }
        }

        stage('Publish Local - Kotlin') {
            steps {
                sh "./gradle.sh publishToMavenLocal"
            }
        }

        stage('Test - Scala') {
            steps {
                sh "sbt -DenableCoverage=true -DgenerateStoryReport=true ';set every enableFatalWarnings := true; clean; test:test'"
            }
            post {
                always {
                    sh "sbt -DenableCoverage=true coverageReport"
                    sh "sbt coverageAggregate"
                    junit testResults: '**/target/test-reports/*.xml', allowEmptyResults: true
                    publishHTML(target: [
                            allowMissing         : true,
                            alwaysLinkToLastBuild: false,
                            keepAll              : true,
                            reportDir            : './target/scala-2.12/scoverage-report',
                            reportFiles          : 'index.html',
                            reportName           : "Scoverage Report"
                    ])

                    publishHTML(target: [
                            alwaysLinkToLastBuild: false,
                            keepAll              : true,
                            reportDir            : './target/testStoryMapping/',
                            reportFiles          : 'index.html',
                            reportName           : "Test story mapping"
                    ])
                }
            }
        }

        stage('Git Tag') {
            steps {
                sh "git tag v$VERSION"
                sh "git push origin v$VERSION"
            }
        }

        stage('Github Release') {
            steps {
                sh "sbt -Dprod.publish=true githubRelease"
            }
        }

        stage('Docs Publish') {
            steps {
                sh "sbt -Dprod.publish=true clean ghpagesPushSite"
            }
        }

        stage('Bintray Publish - Kotlin') {
            steps {
                sh "./gradlew publish"
            }
        }

        stage('Bintray Publish - Scala') {
            steps {
                sh "sbt -Dprod.publish=true -DenableCoverage=false publish"
            }
        }
    }
    post {
        always {
            script {
                sendNotification(currentBuild.result)
            }
        }
    }
}

def sendNotification(String buildStatus = 'STARTED') {
    buildStatus = buildStatus ?: 'SUCCESS'

    def colorCode = '#FF0000'
    def subject = "${buildStatus}: Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]'"
    def summary = "${subject} (${env.BUILD_URL})"
    def details = '${JELLY_SCRIPT,template="html"}'

    // Override default values based on build status
    if (buildStatus == 'STARTED') {
        color = 'YELLOW'
        colorCode = '#FFFF00'
    } else if (buildStatus == 'SUCCESS') {
        color = 'GREEN'
        colorCode = '#00FF00'
    } else {
        color = 'RED'
        colorCode = '#FF0000'
        emailext(
                subject: subject,
                body: details,
                to: "tmt-csw@thoughtworks.com"
        )
    }

    slackSend(channel: "#esw-prod-release", color: colorCode, message: summary)
}
