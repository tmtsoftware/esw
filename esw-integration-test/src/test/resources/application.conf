csw-networks.hostname.automatic = on

scripts {
  testSequencerId1 {
    testObservingMode1 {
      scriptClass = esw.ocs.testData.TestScript
      prefix = esw.ocs.prefix1
    }
  }

  testSequencerId2 {
    testObservingMode2 {
      scriptClass = esw.ocs.testData.TestScript
      prefix = esw.ocs.prefix2
    }
  }

  testSequencerId3 {
      testObservingMode3 {
        scriptClass = esw.ocs.testData.InvalidScript
        prefix = esw.ocs.prefix3
      }
    }

    testSequencerId4 {
      testObservingMode4 {
        scriptClass = esw.ocs.testData.TestScript
        prefix = esw.ocs.prefix4
      }
    }
}
