include "logging.conf"
csw-networks.hostname.automatic = on

akka {
  loglevel = error
}

csw-logging {
  logLevel = fatal
  slf4jLogLevel = fatal
  akkaLogLevel = fatal
  appender-config {
    stdout {
      color = false
      pretty = false
    }
  }
}
