include "logging.conf"

scripts {
  NSCU {
    darknight {
      scriptClass = esw.ocs.app.wiring.ValidTestScript
      prefix = esw.ocs.prefix1
    }
  }
  TCS {
    DarkNight {
      scriptClass = esw.ocs.app.wiring.ValidTestScript
      prefix = esw.ocs.prefix1
    }
  }
}
